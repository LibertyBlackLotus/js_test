/**
 * insertion sort
 */

/*
function insertionSort(arr){
	for(let i = 1; i < arr.length; i ++){
		let x = arr[i];
		for(let j = i - 1; arr[j] > x; j --){
			arr[j+1] = arr[j];
		}
		if(arr[j+1] != x){
			arr[j+1] = x;
		}
	}
	return arr;
}

let arr = [2, 3, 4, 2, 5, 8, 10, 20, 100, 90];
console.log(insertionSort(arr));*/
